# Modules

Each module lives in its own directory as it lands: specification text, fixtures, and a README stating the bounded proposition it supports and its current V0-V3 workflow level.

| Directory | Module | Status |
|-----------|--------|--------|
| `fingerprint-schemes/` | Fingerprint schemes | Not started - awaiting the PR #21 split from the standard repo |
| `seeded-observations/` | VPTS seeded observations | Not started - founding proposal on standard issue #18 |
| `recomputable-attribution/` | Recomputable attribution | Not started - prior work referenced from standard issue #19 |
| `entitlement-credentials/` | Entitlement credentials | Not started - draft to be re-filed from standard PR #34 |

A module directory appears when its first draft is filed; empty placeholders are not committed.
